# Superseded materials

These files preserve the pre-upgrade repository state for audit history only. They
are not imported, executed, or validated by the current reproduction suite.

The legacy monetary-loss script combines unmatched asset, exposure, period, and
reporting perimeters. Its RVR outputs are arithmetic scenarios, not empirical
estimates. The legacy enforcement file conflates clearance and conviction and uses
unsupported probability ranges. The legacy `GF(2^256)` code names a polynomial
without supplying evidence of irreducibility or a complete interoperable field
profile. Do not cite or deploy these outputs.

Git history already preserves the original provenance. This directory can be
removed in a later maintenance commit after reviewers no longer need side-by-side
inspection.

